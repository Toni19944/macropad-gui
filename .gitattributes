# Normalize text files to LF in the repository; Git converts per platform
# on checkout. The golden round-trip test is line-ending agnostic (it
# detects and replicates whatever the fixture uses), so uniform normalized
# endings are all it needs.
* text=auto

# Windows batch files must stay CRLF to run correctly.
*.bat text eol=crlf
