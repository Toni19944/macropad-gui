# --- Upstream tool (not part of this repo) -------------------------------
# The ch57x-keyboard-tool source is a separate project; clone it yourself
# (see README). A byte-for-byte copy of its example-mapping.yaml lives in
# tests/fixtures/ so the test suite stays self-contained.
/ch57x-keyboard-tool/

# --- Python build / install artifacts ------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
*.egg
pip-wheel-metadata/

# --- Virtual environments ------------------------------------------------
.venv/
venv/
env/
ENV/

# --- Test / tooling caches -----------------------------------------------
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# --- Editor / OS cruft ---------------------------------------------------
*~
*.un~
*.swp
*.swo
.DS_Store
Thumbs.db
.idea/
.vscode/

# --- Spec-kit / Claude Code tooling --------------------------------------
# Framework machinery and design docs used to develop this project (the
# spec-driven workflow + Claude Code skills); not part of the shipped tool.
/.specify/
/.claude/
/specs/

# --- Local scratch -------------------------------------------------------
New Text Document.txt
smoke_*.py
